version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/install
      - cypress/run:
          start: npm run dev
          wait-on: 'http://localhost:8080'
          requires:
            - cypress/install
          parallel: true
          parallelism: 2
          group: 2 machines